module clear(state,match,in,keypadBuf);

input state,match;
input [3:0] in;
output [3:0] keypadBuf;	

always@(state or match)
begin
	if(state == 1 || match == 1)
		begin
			keypadBuf <= 4'h0;
		end
		else
		begin
			keypadBuf <= keypadBuf;
		end
end
endmodule		
